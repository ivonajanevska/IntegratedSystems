@model IEnumerable<LibraryDomain.Domain.Book>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<div class="container">
    <div class="row row-cols-3 justify-content-center">
        @foreach(var book in Model)
        {
            <div class="col-3">
                <a class="text-decoration-none text-black" asp-action="Details" asp-route-id="@book.Id">
                    <div class="card" style="width: 18rem;">
                        <img src="@book.Image" class="card-img-top" alt="...">
                        <div class="card-body">
                            <h5 class="card-title">@book.Title</h5>
                            <p class="card-text">@book.Author</p>
                            <div class="row mt-2">
                                <a asp-action="AddBookToCart" asp-controller="Books" asp-route-id="@book.Id" class="btn btn-primary"> Add To Cart</a>
                            </div>
                            <div class="row mt-2 justify-content-between">
                                <a asp-action="Edit" asp-route-id="@book.Id" class="btn btn-primary">Edit</a>
                                <a asp-action="Delete" asp-route-id="@book.Id" class="btn btn-danger">Delete</a>
                            </div>
                        </div>
                    </div>
                </a>
            </div>
            
        }
    </div>
</div>



@* <table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Author)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedOn)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Author)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedOn)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table> *@
